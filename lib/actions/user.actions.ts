"use server";

export const signIn = async () => {
  try {
    // Mutations / Database / Fetch
  } catch (error) {
    console.error("Error", error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // Create User Account
  } catch (error) {
    console.error("Error", error);
  }
};
